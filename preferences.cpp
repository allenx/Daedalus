#include "preferences.h"

Preferences::Preferences() :
    rowCount(0),
    colCount(0),
    mineCount(0)
{
    //QObject::connect(this, SIGNAL(accepted()), this, SLOT(accept()));
    printf("fuck");
}


